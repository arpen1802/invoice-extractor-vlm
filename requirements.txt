# ---------------------------------------------------------------------------
# Core (CPU-friendly) — install these for scaffold, data, metrics, baseline,
# and serving/demo code work. Runs on macOS/Linux without a GPU.
#   pip install -r requirements.txt
# ---------------------------------------------------------------------------
pydantic==2.9.2
datasets==3.0.1
pillow==10.4.0
numpy==1.26.4
python-dateutil==2.9.0.post0

# Synthetic data generation
faker==30.1.0
reportlab==4.2.2
pdf2image==1.17.0            # needs system 'poppler' (brew install poppler)

# Baseline comparison (general LLM API, zero-shot)
openai==1.51.0
anthropic==0.36.2

# Evaluation / experiment tracking
scikit-learn==1.5.2          # bipartite matching helpers for line-item F1
wandb==0.18.3

# Serving + demo (API layer runs on CPU; model inference wants a GPU — see below)
fastapi==0.115.0
uvicorn==0.31.0
gradio==4.44.0

# ---------------------------------------------------------------------------
# GPU-only (NVIDIA CUDA). Do NOT expect these to install/run on a Mac.
# Install on the rented/cloud GPU host for Milestones 3-5.
#   pip install -r requirements-gpu.txt   (or uncomment below on the GPU box)
# ---------------------------------------------------------------------------
# torch==2.4.1                # match the CUDA build for your host
# transformers==4.45.2
# peft==0.13.0
# bitsandbytes==0.44.1        # 4-bit QLoRA — CUDA only
# accelerate==1.0.1
# vllm==0.6.2                 # serving + guided decoding
# outlines==0.1.1             # constrained JSON decoding (alt: xgrammar)
# qwen-vl-utils==0.0.8        # Qwen2.5-VL image preprocessing helpers
